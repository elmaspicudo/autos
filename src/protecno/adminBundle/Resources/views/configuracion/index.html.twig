{% extends '::base.html.twig' %}
{% block placeholder -%}
<div class="row">
    <div class="col-lg-12">
        <ol class="breadcrumb">
            <li><a href="#">Home</a></li>
            <li class="active"><span>Configuracion</span></li>
        </ol>
        <div class="clearfix">
            <h1 class="pull-left">Configuracion</h1>
            <div class="pull-right top-page-ui">
                <a href="{{ path('admin_configuracion_new')}}" class="btn btn-primary pull-right">
                    <i class="fa fa-plus-circle fa-lg"></i> Agregar configuracion
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock -%}
{% block body -%}
<div class="row">
<div class="col-lg-12">
<div class="main-box no-header clearfix">
<div class="main-box-body clearfix">
<div class="table-responsive">
<table class="table user-list table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titulo</th>
                <th>Descripcion</th>
                <th>Metatags</th>
                <th>Terminos</th>
                <th>Aviso</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_configuracion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.titulo }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.metatags }}</td>
                <td>{{ entity.terminos }}</td>
                <td>{{ entity.aviso }}</td>
                <td>
                        <a href="{{ path('admin_configuracion_show', { 'id': entity.id }) }}"class="btn btn-primary">ver</a>
                        <a href="{{ path('admin_configuracion_edit', { 'id': entity.id }) }}" class="btn btn-success">Editar</a>
                    </td>
            </tr>
        {% endfor %}
        </tbody>
        </table>
</div>
</div>
</div>
</div>

       
    {% endblock %}
